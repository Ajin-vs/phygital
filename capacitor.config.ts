import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'phygital-ui',
  webDir: 'dist/phygital_ui',
  bundledWebRuntime: false
};

export default config;
